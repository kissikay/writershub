<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Posts</title>
</head>
<body>
  <% if (blogs && blogs.length > 0) { %>
    <% blogs.forEach(blog => { %>
      <%- include("partials/postCard", { blog }) %>
    <% }) %>
  <% } else { %>
    <p>No blogs found.</p>
  <% } %>
</body>
</html>
